use bevy::prelude::*;

/// Gameplay track mesh rendering (placeholder until a real mesh path exists).
pub struct TrackRenderPlugin;

impl Plugin for TrackRenderPlugin {
    fn build(&self, _app: &mut App) {}
}
